def return_squares(l: list[int]) -> list[int]:
    """_summary_

    Args:
        l (list[int]): _description_

    Returns:
        list[int]: _description_
    """
    # use l and r pointers
    
    # if r > abs l
    return list(0 for _ in range(len(l)))