def dijkstras_shortest_path(nodes : list[list[int]])-> list[int]:
    
    ans = [-1 for i in range(len(nodes))]
    
    
    for i in range(len(nodes)):
        for j in range(len(nodes)):
            ...
    
    return ans